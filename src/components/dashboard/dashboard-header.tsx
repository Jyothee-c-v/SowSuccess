
"use client";

// This component is effectively replaced by the main Navbar when on dashboard pages.
// Returning null removes it from the layout.
export function DashboardHeader() {
  return null;
}
